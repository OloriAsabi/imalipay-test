import React from 'react';

const Title = ({ title }) => (
  <div className=" mb-10">
    <p className="text-3xl font-normal tracking-tight text-slate-900">
      {title}
    </p>
  </div>
);

export default Title;